import React from 'react';

const Home = (props) => {
    return (
       <p >
       <b style={{fontSize: 26}}>DRF-JSON example API explorer.</b> <br/>
       Click on the menu to explore.
       </p>
    )
}

export default Home;
